# Миграция к универсальному UI рендереру

## Проблема

Текущий `src/components/UI/index.tsx` содержит огромный switch-case с явным перечислением всех компонентов (~80+ компонентов). Это создает проблемы:

- Нужно вручную добавлять каждый новый компонент
- Сложно поддерживать и расширять
- Нет автоматической типизации
- Дублирование кода

## Решение

Реализована система автоматической регистрации компонентов с двумя уровнями:

1. **Build-time registry** - компоненты собираются автоматически на этапе сборки
2. **Runtime registry** - компоненты можно регистрировать динамически

## Новая архитектура

### 1. Автоматическая регистрация на этапе сборки

```typescript
// Скрипт scripts/generateComponentRegistry.js автоматически:
// 1. Сканирует папки с компонентами
// 2. Находит компоненты с метаданными
// 3. Генерирует оптимизированную карту компонентов
// 4. Создает файл src/registry/GeneratedComponentMap.ts
```

### 2. Универсальный рендерер

```typescript
// src/components/UI/UniversalUIRenderer.tsx
// Заменяет switch-case на умный поиск компонентов
```

### 3. Новый UI компонент

```typescript
// src/components/UI/index.new.tsx
function UI({ uiConfig, setUiAjaxConfiguration, eventEmitter, dataTransform }) {
    return (
        <UniversalUIRenderer
            uiConfig={uiConfig}
            setUiAjaxConfiguration={setUiAjaxConfiguration}
            eventEmitter={eventEmitter}
            dataTransform={dataTransform}
            fallbackToRegistry={true}
            enableValidation={false}
            enableDevMode={process.env.NODE_ENV === 'development'}
        />
    );
}
```

## Как мигрировать существующие компоненты

### Способ 1: Добавить метаданные в существующие файлы

```typescript
// В существующий компонент (например, TextCard/index.tsx)
// Добавить комментарии для автоматического сканирования:

// @AutoRegister('TextCard')
// @displayName('Text Card')
// @category('text')
const TextCard = ({ data }) => {
    // ... существующая логика
};

// ИЛИ добавить метаданные к компоненту:
(TextCard as any).__pieCardName = 'TextCard';
(TextCard as any).__pieCardMetadata = {
    displayName: 'Text Card',
    category: 'text'
};

export default TextCard;
```

### Способ 2: Использовать PieRegister для новых компонентов

```typescript
import PieRegister from 'pieui';

interface MyCardProps {
    data: {
        title: string;
        description?: string;
    };
}

const MyCard: React.FC<MyCardProps> = ({ data }) => (
    <div>{data.title}</div>
);

const RegisteredMyCard = PieRegister<MyCardProps>(MyCard, {
    name: 'MyCard',
    displayName: 'My Custom Card',
    category: 'custom'
});

export default RegisteredMyCard;
```

## Этапы миграции

### Этап 1: Запустить генерацию реестра

```bash
# Генерировать реестр компонентов
bun run generate:registry

# Проверить результат
cat src/registry/GeneratedComponentMap.ts
```

### Этап 2: Обновить компоненты (постепенно)

Добавлять метаданные к существующим компонентам:

```typescript
// В каждый *Card/index.tsx файл добавить:
(ComponentName as any).__pieCardName = 'ComponentName';
(ComponentName as any).__pieCardMetadata = {
    displayName: 'Component Display Name',
    category: 'category-name'
};
```

### Этап 3: Заменить UI компонент

```typescript
// Заменить src/components/UI/index.tsx на новую версию
import UniversalUIRenderer from './UniversalUIRenderer';

function UI(props) {
    return <UniversalUIRenderer {...props} fallbackToRegistry={true} />;
}
```

### Этап 4: Тестирование

```typescript
// Включить dev mode для отладки
<UniversalUIRenderer
    {...props}
    enableDevMode={true}  // Показывает источник компонента
    enableValidation={true} // Валидирует пропсы
/>
```

## Преимущества новой системы

### ✅ Автоматизация
- Компоненты регистрируются автоматически на этапе сборки
- Нет необходимости вручную обновлять switch-case
- Скрипт генерации находит и регистрирует все компоненты

### ✅ Типизация
- Полная поддержка TypeScript
- Схемы пропсов с валидацией
- Runtime проверка типов (опционально)

### ✅ Гибкость
- Build-time регистрация для стабильных компонентов
- Runtime регистрация для динамических компонентов
- Поддержка fallback компонентов

### ✅ Отладка
- Dev режим с подробной информацией
- Валидация пропсов с понятными ошибками
- Список доступных компонентов при ошибках

## Обратная совместимость

Новая система полностью совместима с существующим API:

```typescript
// Старый способ - продолжает работать
<UI uiConfig={{ card: 'TextCard', data: { text: 'Hello' } }} />

// Новый способ - работает автоматически через универсальный рендерер
<UniversalUIRenderer uiConfig={{ card: 'MyNewCard', data: { title: 'Hello' } }} />
```

## Следующие шаги

1. ✅ Система создана и готова к использованию
2. 🔄 Добавить метаданные к существующим компонентам (постепенно)
3. 🔄 Протестировать генерацию реестра: `bun run generate:registry`
4. 🔄 Заменить старый UI компонент на универсальный рендерер
5. 🔄 Добавить новые компоненты через PieRegister

Новая система готова к использованию уже сейчас!